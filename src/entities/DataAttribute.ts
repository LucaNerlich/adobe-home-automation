/**
 * DataAttribute Interface
 *
 * This interface defines the structure of DataAttribute objects which includes properties like,
 * - `type`: A string property to denote the type of the attribute.
 * - `value`: A string property to store the value of the attribute.
 */
export interface DataAttribute {
    type: string,
    value: string,
}
